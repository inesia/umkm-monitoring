# Project Rules & Guidelines

- **Build & Git Push Rule**: DO NOT automatically run `npm run build` or `git push` after completing changes unless explicitly ordered by the user. Always show/explain the changes first and wait for the user's explicit command to build and push.
